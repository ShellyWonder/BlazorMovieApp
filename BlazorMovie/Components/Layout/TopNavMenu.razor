<nav class="navbar navbar-expand-lg navStyle" data-bs-theme="dark">
    <div class="container-fluid">
        <a href="/" class="me-3"><img src="/img/smallLogo.png" height="50" /></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarBM" aria-expanded="false" aria-label="toggle-navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarBM">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <NavLink class="nav-link" href="/" Match="NavLinkMatch.All">Popular Movies</NavLink>
                   
                </li> 
                <li class="nav-item">
                    <NavLink class="nav-link" href="/now_playing">Now Playing</NavLink>
                    
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="/top_rated">Top Rated</NavLink>
                    
                </li>
                 <li class="nav-item">
                    <NavLink class="nav-link" href="/upcoming">Coming Soon</NavLink>
                   
                </li>

            </ul>
        <SearchComponent  OnMultipleResultsFound="ShowSearchResultsModal"/>
        
        <SearchResultsComponent @ref="searchResultsModal" SearchResults="@searchResults"/>
        </div>
       
    </div>
</nav>

@code {
    private IEnumerable<MovieDetails>? searchResults;
    private SearchResultsComponent? searchResultsModal;

    private void ShowSearchResultsModal(IEnumerable<SearchModel> results)
    {
        searchResults = results
             .Select(sm => sm.MovieDetails)
             .Where(md => md != null) // Exclude null MovieDetails
             .Cast<MovieDetails>(); // Cast to non-nullable MovieDetails

        searchResultsModal?.ShowModal();
    }
}
